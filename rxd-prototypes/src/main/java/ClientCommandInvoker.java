import rx.Observable;
//import rxplay.domain.Domain.CreateCommandStream;

public class ClientCommandInvoker {
  public <Request, Result> Observable<Result> invoke(Request request) {
//    CreateCommandStream cmdStreamCreate = serialiseRequest(request);
//    transport.filter()
//    transport.send(cmdStreamCreate);
    return null;
  }
}
